"""
@generated by mypy-protobuf.  Do not edit manually!
isort:skip_file
"""

from google.protobuf import descriptor as _descriptor
from google.protobuf.internal import enum_type_wrapper as _enum_type_wrapper
import builtins as _builtins
import sys
import typing as _typing

if sys.version_info >= (3, 10):
    from typing import TypeAlias as _TypeAlias
else:
    from typing_extensions import TypeAlias as _TypeAlias

DESCRIPTOR: _descriptor.FileDescriptor

class _StreamMode:
    ValueType = _typing.NewType("ValueType", _builtins.int)
    V: _TypeAlias = ValueType  # noqa: Y015

class _StreamModeEnumTypeWrapper(_enum_type_wrapper._EnumTypeWrapper[_StreamMode.ValueType], _builtins.type):
    DESCRIPTOR: _descriptor.EnumDescriptor
    unknown: _StreamMode.ValueType  # 0
    """Unspecified / default value.
    Callers should treat this as invalid and use defaults.
    """
    updates: _StreamMode.ValueType  # 2
    """Emit only the node/task names and the updates they return after execution.
    If multiple updates happen in the same step (e.g. multiple nodes run),
    each update is emitted as a separate event.
    """
    checkpoints: _StreamMode.ValueType  # 3
    """Emit events whenever a checkpoint is created, in the same shape as
    returned by get_state().
    """
    tasks: _StreamMode.ValueType  # 4
    """Emit events when tasks start and finish, including their results and
    any associated errors.
    """
    debug: _StreamMode.ValueType  # 5
    """Emit a superset of events useful for debugging, including task, checkpoint,
    result, and other internal events.
    """
    messages: _StreamMode.ValueType  # 6
    """Emit only message-related events using the "messages" stream mode.
    In this mode, messages/metadata, messages/partial, and messages/complete are
    represented as separate EventType values
    (messages_metadata / messages_partial / messages_complete).
    """
    custom: _StreamMode.ValueType  # 7
    """Emit custom events written explicitly via the stream_writer."""
    events: _StreamMode.ValueType  # 8
    """Emit LangChain-style callback events such as
    on_{retriever|chain|tool|llm}_{start|end|error}, etc.
    """
    messages_tuple: _StreamMode.ValueType  # 9
    """Emit message tuples using the "messages-tuple" stream mode.
    In this setting, metadata is returned alongside the message itself
    (no separate metadata events), and we do not distinguish between
    partial and complete messages.
    """

class StreamMode(_StreamMode, metaclass=_StreamModeEnumTypeWrapper):
    """This is the public subset of stream modes.
    If you add a new one, update `InternalStreamMode` too.
    SCRIPT: remove the generated String()
    StreamMode controls which categories of events are emitted on the stream.
    Each mode corresponds to a subset of EventType values.
    """

unknown: StreamMode.ValueType  # 0
"""Unspecified / default value.
Callers should treat this as invalid and use defaults.
"""
values: StreamMode.ValueType  # 1
"""Emit all values in the state after each superstep, including interrupts.
This is the most verbose mode and mirrors the `values` EventType.
"""
updates: StreamMode.ValueType  # 2
"""Emit only the node/task names and the updates they return after execution.
If multiple updates happen in the same step (e.g. multiple nodes run),
each update is emitted as a separate event.
"""
checkpoints: StreamMode.ValueType  # 3
"""Emit events whenever a checkpoint is created, in the same shape as
returned by get_state().
"""
tasks: StreamMode.ValueType  # 4
"""Emit events when tasks start and finish, including their results and
any associated errors.
"""
debug: StreamMode.ValueType  # 5
"""Emit a superset of events useful for debugging, including task, checkpoint,
result, and other internal events.
"""
messages: StreamMode.ValueType  # 6
"""Emit only message-related events using the "messages" stream mode.
In this mode, messages/metadata, messages/partial, and messages/complete are
represented as separate EventType values
(messages_metadata / messages_partial / messages_complete).
"""
custom: StreamMode.ValueType  # 7
"""Emit custom events written explicitly via the stream_writer."""
events: StreamMode.ValueType  # 8
"""Emit LangChain-style callback events such as
on_{retriever|chain|tool|llm}_{start|end|error}, etc.
"""
messages_tuple: StreamMode.ValueType  # 9
"""Emit message tuples using the "messages-tuple" stream mode.
In this setting, metadata is returned alongside the message itself
(no separate metadata events), and we do not distinguish between
partial and complete messages.
"""
Global___StreamMode: _TypeAlias = StreamMode  # noqa: Y015
